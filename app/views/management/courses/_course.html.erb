<tr>
  <td><%= course_counter + 1 %></td>
  <td><%= course.name %></td>
  <td><%= course.full_categories %></td>
  <td><%= course.full_duration %></td>
  <td class="text-right"><%= currency_format course.tuition_fee %></td>
  <td>
    <%= link_to t(".edit"), edit_management_course_path(course) %>
  </td>
</tr>
