<hr class="mt-2 mb-2">
<% if user_signed_in? %>
  <%= render "shared/reviews/new_comment", review: review, new_comment: new_comment %>
<% end %>
<%= render partial: "shared/comments/comment", collection: comments, as: :comment %>
