<div>
  <%= link_to center.name, center_path(center.route_params) %>
  <div class="pull-right"><%= summary_rating_format center.summary_rating_cached %></div>
  <br/>
  <%= render "branch_results", center: center, branches: center.branches %>
  <br/>
  <%= t ".#{center.count_course > 1 ? 'price_from' : 'price'}" %>:
  <%= currency_format center.min_course_price %>
</div>
<hr/>
