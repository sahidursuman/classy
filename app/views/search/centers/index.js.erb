$("#center_search_result").html("<%= j render 'search_results', centers: @search.centers, \
  search_form: @search_form %>");
$("#center_pagination").html("<%= j paginate @search.centers, remote: true %>");
