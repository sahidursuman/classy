<% if center_results.present? %>
  <ul>
    <%= render partial: "center_result", collection: center_results, as: :center_result,
      locals: {search_form: search_form} %>
  </ul>
<% else %>
  <%= t ".not_found" %>
<% end %>
