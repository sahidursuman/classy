<% if branches.size == Settings.center_search.max_branches_on_result %>
  <% branches.first.tap do |branch| %>
    <%= branch.full_address %>
  <% end %>
<% else %>
  <%= t ".number_of_branch_in_cities", number_of_branch: branches.size,
    cities: center.city_names %>
<% end %>
